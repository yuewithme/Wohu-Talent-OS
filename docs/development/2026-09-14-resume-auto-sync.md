# BOSS 消息附件简历自动同步飞书

## 任务目标

按用户最新需求，打开 BOSS 后自动运行插件，识别消息中收到的附件简历并上传到指定飞书多维表格。建立 Git 仓库和功能分支，按可验证阶段提交。目标转为消息附件同步，不再以手动读取推荐候选人为本次交付中心。

## 实施计划

1. 保存 v0.2.1 Git 基线，在功能分支开发。
2. 核对真实消息事件、候选人身份、附件下载来源；确认自动处理范围和目标表结构。
3. 实现自动监听、持久化任务队列、幂等去重、附件上传及记录写入、有限重试、配置和状态展示。
4. 在授权范围内实测收到附件、同步成功、重复事件、重启与失败恢复；验证包并提交 Git。

## 完成标准

已配置后打开 BOSS 自动开始监听，收到简历后无需手动读取；同一消息附件只创建一次目标记录，表内附件可打开；失败原因和重试可见。真实简历、密钥、下载 URL 和本地运行数据不进入 Git。缺少目标表、认证或真实样本时如实标记未完成项。

## 完成结果与验证

实施中。用户确认所有会话的新附件与历史补录，使用已指定飞书 Wiki 对应多维表格，并授权调整字段。为多电脑分发，用户确认插件＋统一云端服务；放弃本机连接器方向。

- Git 基线：76d46b6，标签 v0.2.1；分支 feat/boss-resume-feishu-sync。
- 已以现有 SSH 密钥连接 Ubuntu 云服务器，Docker 可用。现有容器不改动，部署使用独立目录和端口。
- 真实消息来源：聊天运行时提供联系人，historyMsg 提供分页历史；只识别入站 hyperLinkType=9 的附件卡片，不将在线微简历当附件。
- 真实 PDF 验证：预览检查返回可访问权限与附件授权 ID；原始下载入口为 docdownload.zhipin.com/wflow/zpgeek/download/download4boss/。授权查询和下载留在浏览器，BOSS Cookie 和签名下载地址不传给云服务。
- 当前联系人列表声明近 30 天范围，历史扫描必须保留平台范围提示，不能承诺获取平台未暴露的更早联系人。
- v0.3.0 已实现后台会话扫描、分页轮转、入站附件识别、持久任务与错误重试、服务端 SQLite 队列、飞书附件上传及记录幂等。后台不再自动加载旧候选人详情采集器。
- 真实 MAIN 采集器验证：当前 5 个联系人均成功查询历史，发现 2 份附件；权限检查生成的下载入口为原始附件下载域名。原始附件的扩展后台下载尚待验证。
- 用户填写的飞书应用密钥已成功换取应用令牌，但字段接口返回 99991672，缺少应用身份权限；已反馈用户。目标表的 10 个同步字段已设置，未写入简历记录。
- 类型检查、生产构建及现有 Node 测试通过（15 项）。已生成 v0.3.0 ZIP；云部署、原始附件下载与飞书最终归档尚未验收。
