# 复用 18083 服务入口

## 任务目标

按用户最新确认，复用 150.158.52.233:18083 现有入口，使其他电脑的插件直接连接云服务，不再依赖本机 SSH 隧道或等待独立 443 入口。

## 实施计划

1. 备份现有 wohu-talent-os 的 Nginx 配置，将简历同步路径代理至已运行的 resume-sync 服务；保留现有静态站点路由。
2. 插件默认填写指定入口，仅为该明确指定的 HTTP origin 添加例外、网站权限及 CSP 配置；访问凭证仍需在各电脑首次配置，不内嵌安装包。
3. 验证公网健康、应用权限、真实附件重复提交、原网站访问和插件连接，更新安装包与使用说明，提交并推送 GitHub。

## 完成标准

从本机通过公网 18083 直接访问同步服务，授权验证成功，已归档附件去重保持有效；关闭本机 SSH 隧道后插件仍能运行。代码、ZIP 和 Git 历史不包含实际访问凭证或简历。

## 完成结果与验证

已接通用户指定的 `http://150.158.52.233:18083`，真实 Edge 已切换到该地址，自动同步和历史附件均启用。本机 SSH 隧道已关闭，插件仍持续扫描；无需使用者安装 SSH。当前 HTTP 入口传输不加密，已向用户说明并写入使用说明。

- 网关：在原 `wohu-talent-os` Nginx 容器添加 `/healthz`、`/v1/` 到 `resume-sync:8787` 的代理。原首页仍为 HTTP 200，长度 13645 字节，ETag 未变。配置备份为服务器 `/home/ubuntu/wohu-resume-sync/server/data/gateway-backups/nginx-20260914T102516Z.conf`；`nginx -t` 通过，热重载成功。
- 插件 v0.3.1：预填 18083 地址，增加该主机权限和精确 HTTP origin 例外，首次安装自动打开设置页。凭证仍由管理员单独提供，每台电脑首次启用并保存；公开 ZIP 不内嵌凭证。
- 实际浏览器：切换地址后重新扫描 5 个会话，2 个附件任务完成，服务端仍只有 2 条 done 任务。停止 PID 20816 的联调 SSH 隧道后，插件最后扫描时间继续更新，无错误；原本机 18787 监听已消失。
- 公网检查：健康接口 200，携带有效凭证的状态检查 200，匿名状态检查 401。独立全新 Chromium 配置中，地址预填正确、凭证为空、自动同步未擅自启用；扩展可直接请求该入口，健康 200、匿名 401。设置页已截图检查，无横向溢出或运行时异常。
- 已有真实归档验收保持有效：2 份附件从飞书重新下载的文件名、SHA256 与原件一致；同一附件重复提交返回原任务和记录，未重复建档。真实归档范围仍受当前 BOSS 账号开放的会话历史限制。
- 验证：`npm run package` 通过类型检查及生产构建；现有 Node 测试 15 项通过，包括精确允许 18083、拒绝其他公网 HTTP 地址。ZIP 的 15 个文件逐项验证 CRC，内容与 dist 及实际加载的 release 一致。
- 分发：`release/wohu-talent-0.3.1.zip`；内部首次连接说明与访问凭证保存在忽略 Git 的 `.local/插件安装与连接说明.txt`。源码和 ZIP 已检查不含实际访问凭证或飞书 Secret；临时诊断页及重载页已移除。
- 运维限制：当前网关变更保存在既有容器中，普通重启保留；若删除重建旧站点容器，需重新执行 `sh scripts/connect-18083.sh`。无需启动可选的 443 网关。
- Git：在 `feat/reuse-18083-sync` 分支实施，完成检查后快进合入 main 并推送；旧 main 的归档分支保持不变。
